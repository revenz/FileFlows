@namespace FileFlows.Client.Components.Common

<FlowTabs @ref="FlowTabs">
    @foreach (var tab in Tabs)
    {
        <FlowTab Title=@tab.Name Visible="@(!tab.Hidden)">
            <FlowPanel Fields=@tab.Fields OnSubmit=@OnSubmit OnClose=@OnClose></FlowPanel>
        </FlowTab>
    }
</FlowTabs>